/// <reference types="cypress" />
import { isMobile } from '../../../support/utils';

describe("Topics Page", () => {
    it("Switch to latest tab", () => {

    });
    it("Switch to rate tab", () => {

    });
    it("Open topic page", () => {

    });
});
